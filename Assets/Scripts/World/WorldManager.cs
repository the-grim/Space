using UnityEngine;
using System.Collections;

public class WorldManager : MonoBehaviour {
	
	public static float G = 12; //This is the global Gravity constant used in the game world. Static means the value isn't unique for every GameObject instance using this script.
	
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
